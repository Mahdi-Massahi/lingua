[project]
name = "lingua"
version = "0.1.0"
description = "an AI powered app for learning languages with AI"
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = [
    "google-adk>=1.22.0",
    "python-dotenv>=1.2.1",
]
